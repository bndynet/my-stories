import { StoryObj } from '@storybook/angular';
import { MatrixComponent } from './matrix.component';

export default {
  title: 'My Stories/Matrix',
  component: MatrixComponent,
  argTypes: {
  }
};


export const CheckBox: StoryObj<MatrixComponent> = {
  
}